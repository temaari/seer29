language: node_js
node_js:
  - 10.16.1
cache: npm

script:
  - yarn test
  - yarn run build

deploy:
  provider: heroku
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  strategy: api
  on:
    branch: master